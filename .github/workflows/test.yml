name: Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]
        ruby-version: ['4.0']
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true

      # - name: Compile native extensions
      #   run: bundle exec rake compile

      - name: Debug - Test Ruby works
        run: ruby -e "puts 'Ruby is working!'"

      - name: Debug - Test native_audio directly
        run: bundle exec ruby -e "require 'native_audio'; puts 'native_audio loaded!'"
        env:
          SDL_AUDIODRIVER: dummy
